#include <stdlib.h>
#include <stdio.h>

/**
 * _setenv - function that changes or adds an environment variable
 * @environ: environment linked list
 * @name:environment name
 * @v: environment value
 * @w: overwrite
 * Return: 0
 */
int _setenv(const char *name, const char *value, int overwrite)
{
	environment_t *penv;
	char *n;
	char *v;
	int w;
	
	environment_t *penvnode;
	char *tmp;

	if (n == NULL || v == NULL)
	{
		return;
	}
	envnode = _setenv(penv, n);

	if (envnode == NULL)
	{
		tmp = (n, v);
		free(tmp);
	}
	else if
	{
		(pw == 1);
		free(envnode->v);
		envnode-> = _strdup(v);
	}
}
