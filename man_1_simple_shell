.TH man 1 "31 JUL 2022" "0.1" "Simple_Shell man page"

.SH NAME
.B Simple_Shell
.Simple shell made in C program to interact with linux operating system.

.SH SYNOPSIS
Simple_Shell

.SH DESCRIPTION
Simple_Shell is a command language  interpreter and wich executes commands read from the standar input or specified file.
.Sh Return Value
Always 0 for success, other number if it fails.

.SH INPUT FILES
Input files that are not supported.

.SS ENVIRONMENT VARIABLES
ENV This variable, when and only when an interactive shell is invoked and the resulting value shall be used as a pathname of file containing shell commands to execute in the current environment. 
.br
*HOME
Determine the pathname of the user's home directory.
br.
*LANG
Provide a default value for the internationalization variables that are unset or null.
.br
*PATH
Establish a string formatted
.br
IP *PWD
This variable shall represent an absolute pathname of the current working directory.Assigment to this variable may be ignored.
.SH

.SH OPTIONS
.B Simple_Shell
Command options will be typed after the main command followed by a space and a '-' symbol.

.SH EXAMPLE

$ ./hs
($) /bin/ls
hsh command_path.c shell.c
($)
($) exit
$

.SH HISTORY
.B hs
is a POSIX-compliant implementation of /bin/sh tha aims to be small as possible.
.B hsh
is a descendant of the first
.B sh
version of Unix appeared in Version 1 AT&T UNIX IN 1971, written by Ken Thompson in AT&T Bell Laboratories.
.B sh
Was rewritten in 1989 under the BSD licence after the Bourne shell from AT&T SystemV Release 4 UNIX, whic inherited the name.
.B sh
It was a simple command interpreter, not designed for scripting, but nontheless introduced several innovative features to the command-line interface and led to the development of the later Unix shell.

.SH SEE ALSO
acces(2), chdir(2), close(2), closedir(2), execve(2), exit(2), fork(2), free(3), getcwd(3), getline(3), isatty(3), kill(2), malloc(3), open(2), perror(3), signal(2), read(2), stat(2), strtok(3), wait(2), write(2).

.SH BUGS
No bugs known.

.SH AUTHORS
armigjoka (Armela Gjoka), viviani22
